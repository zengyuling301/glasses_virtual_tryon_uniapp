# 眼镜试戴功能技术方案评估（MVP · 静态展示）

**文档性质**：技术可行性、实现路径、业务价值及工时粗估，供评审与排期对齐。  
**版本**：1.2  
**日期**：2026-05-12（初稿）  
**最近修订**：2026-05-14（对齐仓库：移动端 Web、试戴锚点、上传与「已戴眼镜」启发式等，见 §9）

---

## 1. 背景与目标

针对眼镜试戴能力，双方从技术可行性、实现路径及业务价值等维度讨论后，确定 **以静态展示为主的 MVP**，核心产品目标为：

- **尺寸匹配预警**：通过拍照识别脸型/脸宽相关特征，推荐或排除不匹配的款式，而非单纯追求「美观展示」。
- **交互简化**：「拍照 → 选镜 → 展示」；重点告知「脸型与眼镜是否不匹配」，不承诺动态、毫米级物理贴合。

---

## 2. 已定技术决策摘要

| 决策项 | 结论 |
|--------|------|
| 高精度 3D 实时渲染 | **不采用**。手机前置景深信息不足、物理尺寸测量复杂度高，与 MVP 成本不匹配。 |
| 主实现路径 | **2D 位图叠加**：人脸关键点定位 → 归一化几何量 → 镜框 PNG 按关键点缩放/旋转/轻量形变 → 静态合成图。 |
| 开源 / 大模型 | **主路径不依赖** Hugging Face Diffusion 或 OpenAI 多模态 API；若需「炫技 Demo」可作**平行对比实验**（见 3.2）。 |
| Demo | 用开源人脸关键点方案快速搭建**静态 Demo**，验证方向与主观效果。 |

---

## 3. 技术实现路径

### 3.1 主路径：2D 叠加（推荐作为 MVP 核心）

1. **输入**：用户正面自拍（首版建议仅正面，降低姿态与标定难度）。
2. **检测**：开源方案如 **MediaPipe Face Landmarker**、**InsightFace** 等，输出人脸关键点或稠密网格。
3. **几何**：基于眼角、鼻翼、颧宽等计算**相对面宽**、**瞳距归一化比例**等，减弱拍摄距离带来的绝对像素误差。（当前实现：**分档与瞳距像素**仍以**虹膜环质心 / 内眦回退**为主；与试戴叠加锚点解耦，见 §9。）
4. **叠加**：每 SKU 提供**带 Alpha 的镜框 PNG**（MVP 一张正面即可）。当前仓库实现为：**眼内外眼角中点**定水平标定与旋转；垂向将「双眼中点」与**鼻梁点（landmark 168 等）**按可配置比例混合，减轻视线偏移带来的镜框上下漂移；`catalog.json` 中仍以 **`pupil_*_frac` / `eyeline_frac`** 标定 PNG 镜片位，可选 **`nose_bridge_blend`**（0～1）调节垂向混合。可选简单仿射/薄板样条等 **2D 形变**（仍非 3D 引擎）。
5. **输出**：静态合成图 + **可解释文案**（如：面宽与镜圈宽度不匹配，不推荐该款）。

**优点**：延迟可控、端侧可部署、商品轮廓可控、与「预警」业务一致。  
**局限**：侧面/大角度姿态效果有限；不宣称毫米级验光级精度。

### 3.2 备选：生成式「图片迁移」（可选 Demo）

- **含义**：Inpainting、ControlNet、IP-Adapter 等，将「佩戴眼镜」生成到人脸区域。
- **风险**：推理成本与延迟高、端侧难部署、镜框**商品还原度**与合规难控；「匹配/不匹配」结论难以**严格规则化**。
- **建议**：若需向客户展示「技术可能性」，可做**左右对比**：左列为规则叠加（正式 MVP），右列为生成式实验，并在文档中明确**商用以左为主**。

### 3.3 Demo 验证计划（可执行）

1. 准备 **50～100 张**授权测试人脸图、**约 10 款**镜框 PNG 及对应元数据样例。
2. 搭建离线脚本或小页面：**关键点 → 叠加 → 输出三联图**（原图 / 合成 / 匹配结论 UI）。
3. 记录：关键点失败率、大角度/遮挡 case、单张处理耗时；**不做**无标定条件下的毫米级真值对标。

**落地情况（截至 2026-05-18）**：第 2 点已在仓库内以 **uni-app 前端 + Flask API + CLI** 形式部分实现。产品主流程在 `uni-app/`（拍照 / 测算 / 试戴工作台）；后端 `demo/app.py` 提供 `/api/analyze`、`/api/tryon` 等接口，推荐区展示 **`/api/frame-preview/<frame_id>`** 镜框缩略图；**同网访问**可通过 `MVP_BIND=0.0.0.0`（默认）与局域网 IP 访问。早期极简 Web 单页（`mvp.html`）已移除。第 1 点仍为 **少量 Demo 素材**；第 3 点的系统化记录与测试集整理**未做**。细则见 **§9 仓库实现进度**。

**参考实现**：`demo/try_on.py`（MediaPipe Face Landmarker + PNG 叠加）、`demo/app.py`（Flask API）、`uni-app/`（试戴前端）、`demo/face_match.py`（分档与预警）、`demo/photo_guard.py`（已佩戴眼镜启发式）、`assets/frames/catalog.json`（SKU 元数据）。用法见根目录 `README.md` 与 `uni-app/README.md`。

---

## 4. 「眼镜匹配」专项方案概要

### 4.1 数据（SKU）

建议每款镜框至少维护：

- 镜圈宽度、鼻梁宽、镜腿长度、镜框几何总宽等；
- 可选：适用瞳距范围、脸宽分档（S/M/L）、性别/风格标签（业务用）。

素材：MVP **单张正面透明 PNG**；后续扩展非本文档范围。

**当前 `catalog.json` 已实现字段（示例）**：`id`、`name`、`band`、`image`（文件名）、`pupil_left_frac` / `pupil_right_frac` / `eyeline_frac`、可选 `nose_bridge_blend`、`mm_total_width`、文案等。

### 4.2 算法与规则（可解释）

- **特征**：从关键点提取 3～5 个稳定相对量（如归一化脸宽、颞部宽度相对瞳距等）。
- **规则示例**：
  - 镜框总宽 vs 归一化脸宽：过窄 / 过宽 → **预警**；
  - 瞳距与镜架光学中心区域偏差过大 → **不推荐**或提示专业验光。
- **输出枚举建议**：`MATCH` | `WARN_TOO_WIDE` | `WARN_TOO_NARROW` | `LOW_CONFIDENCE`（附简短原因）。
- **低置信度**：检测分低、偏转过大、遮挡（口罩、墨镜）时，不输出强结论，引导重拍。

**已实现扩展（工程层）**：分析阶段若启发式判断「**已佩戴眼镜/墨镜**」，API 返回 `error: GLASSES_ON_FACE` 与中文引导（`demo/photo_guard.py`）。该检测为 **Canny/梯度/纹理 + 头姿 stress** 的启发式，非专用分类器：**正脸真镜框**与**歪头/浓妆误报**之间需持续调参**；头滚转 / 左右不对称较大时会 **抬高阈值** 以减少「头稍歪却误报」的情况。

### 4.3 工程架构（示意）

- **端**：采集 + 可选本地推理（如 MediaPipe）或上传人脸图（需隐私与合规方案）。
- **服**：镜框元数据、可选服务端合成（便于水印、统计与 A/B）。

**当前 Demo 服**：本地 Flask；上传单张默认上限约 **40MB**（`MVP_MAX_UPLOAD_MB`），超限 **413** 且对 `/api/*` 返回 **JSON**（避免前端把 HTML 当 JSON 解析报错）。图片解码：`opencv.imdecode` 失败时回退 **Pillow + pillow-heif**（**HEIC**）并做 **EXIF 方向校正**，以兼容 iPhone 相册。

---

## 5. 业务价值与产品表述建议

- **对外**：强调「辅助选款与尺寸风险提示」，避免承诺「与线下试戴完全一致」。
- **对内**：成功指标可侧重「预警合理性」「用户重拍率下降」「选款转化率」等，而非单一主观美观分。

---

## 6. 经验工时（人天 · 粗估）

**假设**：1 名熟悉移动端或 Python 的视觉方向工程师为主力，产品/设计 0.5 人配合；镜框 PNG 与元数据初版由业务方提供。

| 模块 | 内容 | 经验人天 |
|------|------|----------|
| A | 人脸关键点集成、姿态/质量阈值、归一化度量 | 3～5 |
| B | 2D 叠加管线（缩放/旋转/轻量形变）、约 10 款镜框联调 | 3～5 |
| C | 匹配规则引擎、文案、低置信度分支 | 2～4 |
| D | 静态 Demo（脚本或极简 H5）：拍照 / 选镜 / 出图 | 2～4 |
| E | 隐私与合规初稿、异常 case 测试集整理 | 1～2 |
| **MVP 合计** | | **约 11～20 人天**（约 **2.5～4 周** 单人饱和） |

**可选增量**：HF/OpenAI 生成式对比 Demo，**+3～8 人天**（含选型、管线、API/GPU 与效果验收）。

**不含**：大规模设计改图、完整电商后台、法务深度审查；若扩展为**双端正式模块 + 埋点 + 审核**，整体可再乘 **1.5～2** 作粗算。

---

## 7. 风险与依赖

- **无距离标定**：难以输出绝对毫米；以**相对分档 + 预警**为主更稳妥。
- **素材质量**：PNG 透视、比例与真人自拍不一致时，叠加「违和感」上升，需设计规范。
- **合规**：人脸图像存储范围、保留周期、下载水印与告知同意需在方案阶段对齐法务。
- **「已戴眼镜」启发式**：非模型级检测，存在漏检/误检；依赖头姿修正与阈值迭代；产品上宜采用「疑似 + 引导重拍」表述。
- **上传体积与格式**：手机原图体积大易触发网关/应用 **413**；HEIC 需解码链路与依赖维护。

---

## 8. 结论（一句话）

MVP 以 **2D 位图叠加 + 脸型/尺寸可解释规则** 实现「**不匹配预警 + 静态示意图**」；**不采用** 3D 实时与消费级手机景深做物理毫米测量；大模型路线仅作**可选对比 Demo**，不宜作为唯一匹配与商品还原依据。

---

## 9. 仓库实现进度（与 §6 模块粗对照）

以下为 **本仓库已落地能力** 与方案中模块 **A～E** 的对应关系，便于评审「方案 vs 代码」一眼对齐；**非**正式验收清单。

### 9.1 已完成（可用作技术验证 Demo）

| 能力 | 说明 | 对应方案章节 |
|------|------|----------------|
| 人脸关键点 | MediaPipe Face Landmarker（478 点），单人脸检测；**虹膜环质心估瞳距**（分档/低置信），失败时退回内眦 | §3.1、§4.2 |
| 2D 叠加 | 镜框 PNG 按**眼内外眼角中点**定标定旋转 + **眼–鼻梁**垂向混合 + Alpha 合成；`pupil_*_frac`/`eyeline_frac` 对齐 PNG 镜片位，可选 `nose_bridge_blend`；`compose_try_on_bgr` / `compose_try_on_bgr_from_landmarks`（`demo/try_on.py`） | §3.1 |
| 归一化面宽代理 | 颊部轮廓锚点（如 234/454）间距 ÷ **虹膜瞳距**，得到无量纲比例；再分 **S / M / L** 三档 | §3.1、§4.2 |
| 镜架元数据 | `assets/frames/catalog.json` + 透明 PNG；`image` 文件名、`band`、标称宽、文案、`pupil_*_frac`、`eyeline_frac`、可选 `nose_bridge_blend` | §4.1 |
| 可解释匹配状态 | `MATCH`、`WARN_TOO_NARROW`、`WARN_TOO_WIDE`、`LOW_CONFIDENCE`；API 英文枚举，Web 中文标签 | §4.2 |
| 试戴前端 + API | `uni-app/` 主流程；`demo/app.py`：`/api/analyze`、`/api/tryon`、`/api/catalog`、`/api/frame-preview/<frame_id>`；`MVP_BIND` / `MVP_MAX_UPLOAD_MB` 环境变量 | §3.3、§4.3 |
| 上传与解码 | HEIC（`pillow-heif`）+ EXIF 方向；解码失败 JSON `DECODE_FAILED`；413 JSON `PAYLOAD_TOO_LARGE`（`/api/*`） | §4.3 |
| 已佩戴眼镜提示 | `photo_guard.likely_has_eyewear`：多条件 Canny + 融合纹理；**头滚转 / 左右不对称 stress** 抬高阈值；`GLASSES_ON_FACE` | §4.2 |
| 低置信度（初版） | 瞳距在图像中过小（像素阈值）时标为低置信；**未**接 blendshape 姿态/遮挡细分 | §4.2 |

### 9.2 部分完成 / 简化相对方案

| 项 | 方案预期 | 当前仓库 |
|----|-----------|----------|
| 镜框数量与素材 | 约 10 款商品级 PNG + 元数据 | **catalog 示例 3 SKU**（`sample.png` 占位），非商品摄影 |
| 验证与数据 | 50～100 张授权人脸 + 指标记录 | **未建库**；仅依赖开发者自测与 OpenCV 示例图等 |
| Demo 形态 | 三联图（原图 / 合成 / 结论） | **uni-app 分步**（拍照 / 测算 / 试戴工作台），推荐区横向卡片 + 镜框缩略图，非固定三联导出 |
| 匹配规则深度 | 瞳距与镜架光学中心偏差等 | **未**单独实现「光学中心偏差」数值规则；以面宽分档 vs 镜架 `band` 为主 |
| 工程形态 | 端侧或正式服务端 + 合规 | **本地 Flask 开发服**，无持久化存人脸、无埋点/水印/法务流程 |

### 9.3 未做（仍属方案规划或可选增量）

- 生成式试戴对比（§3.2）与左右对比 UI。  
- 系统化的关键点失败率 / 大角度与遮挡 case 台账与耗时统计（§3.3 第 3 点）。  
- 隐私与合规初稿、异常 case 测试集整理（§6 模块 E）。  
- 薄板样条等更强 2D 形变、多 SKU 大规模设计联调（§3.1、§6 模块 B）。  
- 专用「是否戴眼镜」分类模型（替代启发式）。

### 9.4 后续建议（保持与 §7 风险一致）

1. **标定**：用真实授权样本统计颊宽/瞳距分位，重标 S/M/L 阈值，减少误判。  
2. **素材**：替换为商品级透明 PNG，并为每 SKU 校准 `pupil_*_frac` / `eyeline_frac`（及按需 `nose_bridge_blend`），与 `README.md` 调参说明一致。  
3. **合规**：若上线外网或留存用户图，需单独立项对齐告知同意、保留周期与水印等（§7）。  
4. **photo_guard**：若业务强依赖该能力，建议采集样本后改为小模型或商用 API，并保留「疑似」产品话术。

---

## 10. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-05-12 | 初稿落盘 |
| 1.1 | 2026-05-13 | 增加 §3.3 落地说明、§9 仓库实现进度与方案模块对照；版本号与参考实现路径更新 |
| 1.2 | 2026-05-14 | 对齐试戴锚点（眼廓+鼻梁）、Web 移动端与 API（frame-preview、413/JSON、HEIC）、`photo_guard` 与头姿 stress；§4、§7、§9 扩充；修订记录更新 |
