---
description: 境外资源走本机代理；安装依赖优先国内镜像（清华等）
alwaysApply: true
---

# 网络与依赖安装（中国大陆环境）

## 何时启用代理

在访问**境外 pip 默认源**、**国内不可达的网站**、或**明显因网络被阻断**的操作前，在同一 shell 中先导出代理（端口按本机实际为准）：

```bash
export http_proxy=http://127.0.0.1:1087
export https_proxy=http://127.0.0.1:1087
export ALL_PROXY=socks5://127.0.0.1:1080
```

随后再执行 `curl`/`wget`/`git clone`/`pip install`（未改镜像时）等需要出境的命令。若用户已配置系统代理或命令自带镜像，不必重复导出。

## 安装依赖：优先国内镜像

默认优先使用国内源，减少超时与连接错误；仅在用户明确要求或镜像缺包时再改回官方源。

**pip / pip-tools**

```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**uv（示例）**

```bash
UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple uv pip install ...
```

**npm / pnpm / yarn**

```bash
npm install --registry=https://registry.npmmirror.com
# 或 pnpm/yarn 使用 npmmirror 文档中的 registry 地址
```

**Go**

```bash
export GOPROXY=https://goproxy.cn,direct
```

## 原则

- 先镜像、后代理：能走国内镜像的**不要**依赖代理拉包。
- 需要访问被墙站点或官方源且镜像不可用时，再组合使用上面的 `export` 与对应命令。
